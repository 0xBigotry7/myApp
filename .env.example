# Database
# For local development, use a PostgreSQL connection string
# Example: postgresql://user:password@localhost:5432/myapp
# For production (Neon/Vercel), you'll get this from your database provider
DATABASE_URL="postgresql://user:password@localhost:5432/myapp"

# NextAuth
# Generate a secret: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-change-this-in-production"
# For production, set to your deployed URL (e.g., https://your-app.vercel.app)
NEXTAUTH_URL="http://localhost:3000"

# Groq (for AI features - FREE & FAST!)
# Get your FREE API key at: https://console.groq.com/keys
# Used for receipt scanning with Llama 3.2 Vision
GROQ_API_KEY="gsk_your-groq-api-key-here"

# Optional: OpenAI (legacy, can be removed if using Groq)
OPENAI_API_KEY="sk-proj-your-key-here"

# Optional: Anthropic (if using Claude)
ANTHROPIC_API_KEY="your-anthropic-api-key-here"

# Google Drive API (for trip photo storage)
# Create OAuth credentials at: https://console.cloud.google.com/apis/credentials
# 1. Create a new project or select existing
# 2. Enable Google Drive API
# 3. Create OAuth 2.0 Client ID (Web application)
# 4. Add authorized redirect URI: http://localhost:3000/api/auth/google-drive/callback (for dev)
#    and https://your-domain.com/api/auth/google-drive/callback (for production)
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
GOOGLE_REDIRECT_URI="http://localhost:3000/api/auth/google-drive/callback"

# Optional: Vercel Blob Storage (legacy, for AI-generated images only)
# Get your token from: https://vercel.com/dashboard/stores
# Or create via Vercel CLI: vercel blob create
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_your-token-here"

# Optional: UploadThing (for file uploads)
UPLOADTHING_SECRET="your-uploadthing-secret-here"
UPLOADTHING_APP_ID="your-uploadthing-app-id-here"

# Optional: Exchange Rate API
EXCHANGE_RATE_API_KEY="your-exchange-rate-api-key-here-optional"

# Bank Integration (for automatic transaction syncing)
# Plaid - for Bank of America and other US banks
# Get your credentials at: https://dashboard.plaid.com/signup
# Free tier: 100 items in development
PLAID_CLIENT_ID="your-plaid-client-id-here"
PLAID_SECRET="your-plaid-sandbox-secret-here"
PLAID_ENV="sandbox"  # Use 'production' for live banking data

# Wise API - for Wise account syncing
# Users will enter their personal Wise API token in the app UI
# Get token at: https://wise.com/settings/personal-tokens
WISE_ENV="production"  # Use 'sandbox' for testing if available
